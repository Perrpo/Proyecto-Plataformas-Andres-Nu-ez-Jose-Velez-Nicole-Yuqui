<template>
  <aside class="sidebar">
    <div class="logo">
      <span>🍱</span>
      <div>
        <h3>FoodSave</h3>
        <small>Plataforma anti-desperdicio</small>
      </div>
    </div>
    <nav>
      <RouterLink to="/dashboard" active-class="active">Dashboard</RouterLink>
      <RouterLink to="/map" active-class="active">Mapa</RouterLink>
      <RouterLink to="/notifications" active-class="active">
        Notificaciones
        <span class="badge">2</span>
      </RouterLink>
    </nav>
  </aside>
</template>

<script setup lang="ts">
import { RouterLink } from 'vue-router'
</script>

<style scoped>
.sidebar {
  width: 220px;
  background: linear-gradient(180deg, rgba(14, 19, 26, 0.7), rgba(14, 19, 26, 0.85));
  border-right: 1px solid var(--border);
  min-height: 100vh;
  padding: 1.5rem 1rem;
  position: fixed;
  backdrop-filter: blur(6px) saturate(130%);
  box-shadow:
    inset -1px 0 0 rgba(255, 255, 255, 0.04),
    0 10px 30px rgba(0, 0, 0, 0.35);
}
.logo {
  display: flex;
  align-items: center;
  gap: 0.7rem;
  margin-bottom: 2rem;
}
.logo span {
  font-size: 2rem;
  color: var(--neon-cyan);
  filter: drop-shadow(0 0 10px rgba(33, 230, 255, 0.6));
}
.logo h3 {
  margin: 0;
  font-size: 1.1rem;
  color: var(--text-primary);
}
.logo small {
  color: var(--text-secondary);
  font-size: 0.85rem;
}
nav {
  display: flex;
  flex-direction: column;
  gap: 1.2rem;
}
nav a {
  color: var(--text-secondary);
  text-decoration: none;
  font-weight: 500;
  padding: 0.5rem 1rem;
  border-radius: 8px;
  transition:
    background 180ms ease,
    color 180ms ease,
    transform 120ms ease;
  position: relative;
}
nav a.active,
nav a:hover {
  background: linear-gradient(90deg, rgba(33, 230, 255, 0.12), rgba(155, 92, 255, 0.12));
  color: var(--neon-cyan);
  transform: translateX(2px);
  box-shadow:
    inset 0 0 0 1px rgba(33, 230, 255, 0.25),
    0 6px 18px rgba(33, 230, 255, 0.08);
}
.badge {
  background: var(--neon-magenta);
  color: #020409;
  border-radius: 12px;
  font-size: 0.8rem;
  padding: 2px 8px;
  margin-left: 8px;
  box-shadow:
    0 0 0 1px rgba(255, 63, 180, 0.45),
    0 0 18px rgba(255, 63, 180, 0.35);
}
</style>
