{
  "name": "repo",
  "private": true,
  "workspaces": [
    "apps/*",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "scripts": {
    "dev": "concurrently -n web,api -c \"blue,green\" \"npm -w @repo/web run dev\" \"npm -w @repo/backend run dev\"",
    "build": "npm -w @repo/web run build && npm -w @repo/backend run build",
    "type-check": "npm -w @repo/web run type-check",
    "lint": "npm -w @repo/web run lint && npm -w @repo/backend run lint",
    "start": "npm -w @repo/backend run start"
  }
}
